export const API_BASE = ""; // no remote backend for offline 
// set to URL if you add a server

export const ENDPOINTS = {
  // example => `${API_BASE}/auth/login`
  login: `${API_BASE}/auth/login`,
  register: `${API_BASE}/auth/register`,
  notes: `${API_BASE}/notes`,
};
